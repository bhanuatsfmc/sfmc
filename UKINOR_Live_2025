<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="ROBOTS" content="NOINDEX, NOFOLLOW">
    <link rel="stylesheet" type="text/css" href="https://www.smith-nephew.com/assets/head/?v=lCtdyL9hWcNhgOrwD6wZ5SkrHpECjEkDkcl0QP06bh81">
    <link rel="icon" href = "https://image.digital.smith-nephew.com/lib/fe3211737364047e751177/m/1/48107551-6b7e-419f-9ad3-2676a4239229.png">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>
    <script src="https://cloud.digital.smith-nephew.com/datalayer" type="text/javascript"></script>
     <style>
    .sn_form_question_wrapper {
      display: flex;
      margin-bottom: 5px;
      }
   .consent-revealer--cta label {
      width: 100%;
      }
   </style>
   
      <div data-type="slot" data-key="amp" data-label="Add AMPscript here" data-allowed-blocks="codesnippetblock"></div>
    
    
    <script runat="server">
  Platform.Load("Core","1.1.1");
  try{
</script>
  <!--
%%[
/* --- ON PAGE LOAD --- */
set @MID = AuthenticatedMemberID()
SET @RecordTypeId = "0124P000000OSkwQAG"
IF @MID == "536003181" THEN
   /*TEST BU*/
   
   SET @journeyDefKey = "APIEvent-b9ebb4e4-377b-4d3c-d29a-6337668be75a"
   SET @clientid = "dmhkvga4japrzyukhrdc7uad"
   SET @clientSecret = "AfrpPd023zmuWGinvHUusut9"
   SET @authURI = "https://mcyy-6cm08srlbw8b5cqf86346m1.auth.marketingcloudapis.com/"
   SET @restURI = "https://mcyy-6cm08srlbw8b5cqf86346m1.rest.marketingcloudapis.com/"
ELSEIF @MID == "536003183" THEN
   /*Central EU BU*/
   SET @journeyDefKey = "APIEvent-41378b75-2393-1652-6268-5c128ac8befc"
   SET @clientid = "9kpduelrgqgpkw8qx7rdxc5s"
   SET @clientSecret = "qCLgQj7OKLU2MHfMCduRACWc"
   SET @authURI = "https://mcyy-6cm08srlbw8b5cqf86346m1.auth.marketingcloudapis.com/"
   SET @restURI = "https://mcyy-6cm08srlbw8b5cqf86346m1.rest.marketingcloudapis.com/"
ELSEIF @MID == "536003057" THEN
  /* Global BU */
   SET @journeyDefKey = "APIEvent-a1f7ab0c-520c-f863-433a-949b1825445d"
   SET @clientid = "nos89tspp30h2y1x633ld3l0"
   SET @clientSecret = "qDPdyeD8XbKXdrWtcvxZKqGd"
   SET @authURI = "https://mcyy-6cm08srlbw8b5cqf86346m1.auth.marketingcloudapis.com/"
   SET @restURI = "https://mcyy-6cm08srlbw8b5cqf86346m1.rest.marketingcloudapis.com/"
ENDIF

SET @dateAdded = Now(1)
SET @dateAdded = SystemDateToLocalDate(@dateAdded)
SET @embedded = QueryParameter("embedded")
SET @standaloneImg = Iif(Empty(@embedded), "form-background", "")
SET @embeddedcss = Iif(Empty(@embedded), "", "embedded")


 /* --- AFTER SUBMISSION --- */
IF RequestParameter("sn_form_submit") == "submit" THEN

  SET @tempoConsentSource = concat("[Temporary] ",@consentSource)
  SET @display = "ThankYou"
  
  /* All fields in webform */
  SET @email = RequestParameter("sn_form_email")
  SET @firstName= RequestParameter("sn_form_first_name")
  SET @lastName= RequestParameter("sn_form_last_name")
  SET @country= RequestParameter("sn_form_country")
  SET @city= RequestParameter("sn_form_city")
  SET @phone = RequestParameter("sn_form_phone")
  SET @profession = RequestParameter("sn_form_profession")
  SET @specialty = RequestParameter("sn_form_specialty")
  SET @specialtyOther = RequestParameter("sn_form_specialty_other")
  SET @postalcode = RequestParameter("sn_form_postalcode") 
  
  SET @street = RequestParameter("sn_form_street") 
  SET @Company= RequestParameter("sn_form_company")
  SET @marketingconsent = Iif(not Empty(RequestParameter("marketingconsent")), "True", "False")
  SET @title = RequestParameter("sn_form_title")
  
  
  
  /* -- AREA OF INTEREST -- */
  SET @Interestsall = RequestParameter("areaofinterest") 
  SET @audiology = Iif(IndexOf(@Interestsall,"audiology") > 0, "true","false")
  SET @general = Iif(IndexOf(@Interestsall,"general") > 0, "true","false")
  SET @laryngology = Iif(IndexOf(@Interestsall,"laryngology") > 0, "true","false")
  SET @rhinology = Iif(IndexOf(@Interestsall,"rhinology") > 0, "true","false")
  
  SET @jr_foot_and_ankle = Iif(IndexOf(@Interestsall,"jr_foot_and_ankle") > 0, "true","false")
  SET @jr_hand_and_wrist = Iif(IndexOf(@Interestsall,"jr_hand_and_wrist") > 0, "true","false")
  SET @jr_hip = Iif(IndexOf(@Interestsall,"jr_hip") > 0, "true","false")
  SET @jr_knee = Iif(IndexOf(@Interestsall,"jr_knee") > 0, "true","false")
  SET @jr_shoulder = Iif(IndexOf(@Interestsall,"jr_shoulder") > 0, "true","false")
  
  SET @acute_wounds = Iif(IndexOf(@Interestsall,"acute_wounds") > 0, "true","false")
  SET @burns = Iif(IndexOf(@Interestsall,"burns") > 0, "true","false")
  SET @chronic_challenging_wounds = Iif(IndexOf(@Interestsall,"chronic_challenging_wounds") > 0, "true","false")
  SET @dermatology = Iif(IndexOf(@Interestsall,"dermatology") > 0, "true","false")
  SET @plastic_surgery = Iif(IndexOf(@Interestsall,"plastic_surgery") > 0, "true","false")
  SET @podiatry = Iif(IndexOf(@Interestsall,"podiatry") > 0, "true","false")
  SET @surgical_wounds = Iif(IndexOf(@Interestsall,"surgical_wounds") > 0, "true","false")
  
  SET @sm_foot_and_ankle = Iif(IndexOf(@Interestsall,"sm_foot_and_ankle") > 0, "true","false")
  SET @sm_hand_and_wrist = Iif(IndexOf(@Interestsall,"sm_hand_and_wrist") > 0, "true","false")
  SET @sm_hip = Iif(IndexOf(@Interestsall,"sm_hip") > 0, "true","false")
  SET @sm_knee = Iif(IndexOf(@Interestsall,"sm_knee") > 0, "true","false")
  SET @sm_shoulder = Iif(IndexOf(@Interestsall,"sm_shoulder") > 0, "true","false")
  
  SET @lower_limb = Iif(IndexOf(@Interestsall,"lower_limb") > 0, "true","false")
  SET @upper_limb = Iif(IndexOf(@Interestsall,"upper_limb") > 0, "true","false")
  SET @abdominal = Iif(IndexOf(@Interestsall,"abdominal") > 0, "true","false")

  /* Contact and Lead match process */
  SET @contactRows = RetrieveSalesforceObjects("Contact", "Id, Dotdigital_Account__c,Consent_Source__c,Consent_type__c", "Email", "=", @email)
  SET @leadRows = RetrieveSalesforceObjects("Lead", "Id, Dotdigital_Account__c,Consent_Source__c,Consent_type__c", "Email", "=", @email)
  
  /* --- CONTACT --- */
  IF RowCount(@contactRows) > 0 THEN
    SET @contactRow = Row(@contactRows, 1)
    SET @id = Field(@contactRow, "Id")
    SET @consentCRM = Field(@contactRow, "Dotdigital_Account__c")
    SET @consentSourceCRM = Field(@contactRow, "Consent_Source__c") 
    SET @consentTypeCRM = Field(@contactRow, "Consent_type__c") 
    
    SET @userType = "Contact" 
    /* Update information on Contact */
    set @CRMUpdatefunction = concat("UpdateSingleSalesforceObject('Contact',@id,"," ")
    set @FormFields = "audiology,laryngology,general,rhinology,jr_foot_and_ankle,jr_hip,jr_hand_and_wrist,jr_knee,jr_shoulder,acute_wounds,burns,chronic_challenging_wounds,dermatology,plastic_surgery,podiatry,surgical_wounds,sm_foot_and_ankle,sm_hand_and_wrist,sm_hip,sm_knee,sm_shoulder,lower_limb,upper_limb,abdominal"
  
    set @CRMList = "Audiology__c,Laryngology__c,General__c,Rhinology__c,Foot_and_ankle__c,Hip__c,Hand_and_wrist__c,Knee__c,Shoulder__c,Acute_wounds__c,Burns__c,Chronic_Challenging__c,Dermatology__c,Plastic_Surgery__c,Podiatry__c,Surgical_wounds__c,Foot_and_ankle_sm__c,Hand_and_wrist_sm__c,Hip_sm__c,Knee_sm__c,Shoulder_sm__c,Lower_limb__c,Upper_limb__c,Abdominal__c"
  
    set @Formrows = BuildRowsetFromString(@FormFields,",")
    set @rowsCRM = BuildRowsetFromString(@CRMList,",")
    set @rowCount = rowCount(@Formrows)
    if @rowCount > 0 then
      for @i = 1 to @rowCount do
        var @field
        set @row = row(@Formrows, @i)
        set @rowCRM = row(@rowsCRM, @i)

        set @field = field(@row,1)
        set @FieldCRM = field(@rowCRM,1)
        
        SET @ampStr = CONCAT('%%=v(@',@field,')=%%')
        set @FieldValue = TreatAsContent(@ampStr)

        IF (NOT EMPTY(@FieldValue) AND @FieldValue != 'False') AND @i != @rowCount THEN
         SET @CRMUpdatefunction = concat(@CRMUpdatefunction,"'",@FieldCRM,"',@",@field,",")

        ELSEIF (NOT EMPTY(@FieldValue) AND @FieldValue != 'False') AND @i == @rowCount THEN
          SET @CRMUpdatefunction = concat(@CRMUpdatefunction,"'",@FieldCRM,"',@",@field)
        ENDIF
      next @i 

      SET @CRMUpdateLen = length(@CRMUpdatefunction)
      /* 44 is length for empty function (UpdateSingleSalesforceObject('Contact',@id,)); Update only if at least one AOI is selected */
      IF @CRMUpdateLen != 44 THEN
        set @LastCharComma = Substring(@CRMUpdatefunction,@CRMUpdateLen, Subtract(IndexOf(@CRMUpdatefunction,","),1))

        IF @LastCharComma == "," THEN
         SET @CRMUpdatefunction = Substring(@CRMUpdatefunction,1, Subtract(@CRMUpdateLen,1))
        ENDIF

        SET @CRMUpdatefunction = concat(@CRMUpdatefunction,")")
        SET @CRMUpdatefunction = CONCAT('%%=v(',@CRMUpdatefunction,')=%%')
        set @FINALUPDATE = TreatAsContent(@CRMUpdatefunction)
      ENDIF
    ENDIF
  
  /* --- EXISTING LEAD --- */  
  ELSEIF RowCount(@contactRows) == 0 AND RowCount(@leadRows) > 0 THEN
    SET @leadRow = Row(@leadRows, 1)
    SET @id = Field(@leadRow, "Id")
    SET @consentCRM = Field(@leadRow, "Dotdigital_Account__c")
    SET @consentSourceCRM = Field(@leadRow, "Consent_Source__c") 
    SET @consentTypeCRM = Field(@leadRow, "Consent_type__c") 
    set @userType = "Lead"
    
    
   /* Update information on exisitng Lead */
    set @CRMUpdatefunction = concat("UpdateSingleSalesforceObject('Lead',@id,"," ")
    set @FormFields = "firstName,lastName,country,city,phone,profession,specialty,specialtyOther,postalcode,street,Company,title,audiology,laryngology,general,rhinology,jr_foot_and_ankle,jr_hip,jr_hand_and_wrist,jr_knee,jr_shoulder,acute_wounds,burns,chronic_challenging_wounds,dermatology,plastic_surgery,podiatry,surgical_wounds,sm_foot_and_ankle,sm_hand_and_wrist,sm_hip,sm_knee,sm_shoulder,lower_limb,upper_limb,abdominal"
  
  set @CRMList = "FirstName,LastName,Country,City,Phone,Healthcare_Profession__c,Main_Specialty__c,Secondary_Specialty__c,PostalCode,Street,Company,Title,Audiology__c,Laryngology__c,General__c,Rhinology__c,Foot_and_ankle_jr__c,Hip_jr__c,Hand_and_wrist_jr__c,Knee_jr__c,Shoulder_jr__c,Acute_wounds__c,Burns__c,Chronic_Challenging__c,Dermatology__c,Plastic_Surgery__c,Podiatry__c,Surgical_wounds__c,Foot_and_ankle_sm__c,Hand_and_wrist_sm__c,Hip_sm__c,Knee_sm__c,Shoulder_sm__c,Lower_limb__c,Upper_limb__c,Abdominal__c"
  
  set @Formrows = BuildRowsetFromString(@FormFields,",")
  set @rowsCRM = BuildRowsetFromString(@CRMList,",")
  set @rowCount = rowCount(@Formrows)


    if @rowCount > 0 then
      for @i = 1 to @rowCount do
        var @field
        set @row = row(@Formrows, @i)
        set @rowCRM = row(@rowsCRM, @i)

        set @field = field(@row,1)
        set @FieldCRM = field(@rowCRM,1)
        
        SET @ampStr = CONCAT('%%=v(@',@field,')=%%')
        set @FieldValue = TreatAsContent(@ampStr)

        IF (NOT EMPTY(@FieldValue) OR @FieldValue != 'False') AND @i != @rowCount THEN
         SET @CRMUpdatefunction = concat(@CRMUpdatefunction,"'",@FieldCRM,"',@",@field,",")

        ELSEIF (NOT EMPTY(@FieldValue) OR @FieldValue != 'False') AND @i == @rowCount THEN
          SET @CRMUpdatefunction = concat(@CRMUpdatefunction,"'",@FieldCRM,"',@",@field)
        ENDIF
      next @i 

      SET @CRMUpdateLen = length(@CRMUpdatefunction)
      set @LastCharComma = Substring(@CRMUpdatefunction,@CRMUpdateLen, Subtract(IndexOf(@CRMUpdatefunction,","),1))

      IF @LastCharComma == "," THEN
       SET @CRMUpdatefunction = Substring(@CRMUpdatefunction,1, Subtract(@CRMUpdateLen,1))
      ENDIF

      SET @CRMUpdatefunction = concat(@CRMUpdatefunction,")")
      SET @CRMUpdatefunction = CONCAT('%%=v(',@CRMUpdatefunction,')=%%')
      set @FINALUPDATE = TreatAsContent(@CRMUpdatefunction)
    ENDIF

      /* --- NEW LEAD --- */  
  ELSE
   SET @consentTypeCRM = "Single"
   set @userType = "Lead"
   SET @company =  Iif(EMPTY(@company), "[Missing]", @company)
   SET @Id = CreateSalesforceObject("Lead", 41,
                  "RecordTypeId",@RecordTypeId,
                  "Status", "New",
                  "LastName", @lastName, 
                  "FirstName", @firstName,
                  "Email", @email,
                  "Country", @country,
                  "Consent_type__c", @consentTypeCRM,
                  "Dotdigital_Account__c", @marketingconsent,
                  "Lead_Source_Name__c", @LeadSource,
                  "Company",@Company,
                 
                  "City",@city,
                  "PostalCode",@postalcode,
                  "Street",@street,
                  "Healthcare_Profession__c",@profession,
                  "Main_Specialty__c",@specialty,
                  "Secondary_Specialty__c",@specialtyOther,
                  "Phone",@phone,
                  "Title",@title,
                  "Audiology__c", @audiology,
                  "Laryngology__c", @laryngology,
                  
                  "General__c", @general,
                  "Rhinology__c", @rhinology,
                  "Foot_and_ankle_jr__c", @jr_foot_and_ankle,
                  "Hand_and_wrist_jr__c", @jr_hand_and_wrist,
                  "Hip_jr__c", @jr_hip,
                  "Knee_jr__c", @jr_knee,
                  "Shoulder_jr__c", @jr_shoulder,
                  "Acute_wounds__c", @acute_wounds,
                  "Burns__c", @burns,
                  "Chronic_Challenging__c", @chronic_challenging_wounds,
                  
                  "Dermatology__c", @dermatology,
                  "Plastic_Surgery__c", @plastic_surgery,             
                  "Podiatry__c", @podiatry,
                  "Surgical_wounds__c", @surgical_wounds,
                  "Foot_and_ankle_sm__c", @sm_foot_and_ankle,
                  "Hand_and_wrist_sm__c", @sm_hand_and_wrist,
                  "Hip_sm__c", @sm_hip,
                  "Knee_sm__c", @sm_knee,
                  "Shoulder_sm__c", @sm_shoulder,
                  "Lower_limb__c", @lower_limb,
                  
                  "Upper_limb__c", @upper_limb
                )
    ENDIF
    
    /* --- Updates --- */
    
    * Campaign Member process */
    IF @CRMCampaign == "true" AND NOT EMPTY (@campID) THEN
      SET @campUser = concat(@userType,"Id")
      SET @CampMemberRows = RetrieveSalesforceObjects("CampaignMember", "Id", @campUser, "=", @id, "CampaignId", "=", @campID)
     
      IF @UpdateCampaignMemberStatus == "true" AND RowCount(@CampMemberRows) > 0 THEN 
        SET @CampMemberRow = Row(@CampMemberRows, 1)
        SET @CampMemberId = Field(@CampMemberRow, "Id")
        SET @createCampMember = UpdateSingleSalesforceObject(
              "CampaignMember", @CampMemberId,
              "Marketing_Status__c", @marketingstatus,
              "Status", @campStatus
            )

      ELSEIF RowCount(@CampMemberRows) < 1 THEN
        SET @createCampMember = CreateSalesforceObject(
              "CampaignMember", 4,
               @campUser, @id,
              "Status", @campStatus,
              "Marketing_Status__c", @marketingstatus,
              "CampaignId", @campID
            )
      ENDIF
   ENDIF   
   
    
    /* Double optin logic */ 
    IF @DoubleOptinProcess == "true" THEN
      IF (@country == 'Germany' OR @country == 'Switzerland' OR @country == 'Austria') AND @marketingconsent == "True" AND (@consentTypeCRM != "Double" OR @consentTypeCRM != "True") THEN
       SET @needsDoubleOptIn = "True"
      ELSE 
       SET @needsDoubleOptIn = "False"
      ENDIF
    ENDIF
    
    
  IF @TemporaryProcess == "true" THEN
  /* Temporary consent logic */
    IF (@ConsentCRM != "True" AND @marketingconsent == "True") OR (IndexOf(@consentSourceCRM,"Temporary") > 0 AND @marketingconsent == "True") THEN
        SET @updateUser = UpdateSingleSalesforceObject(
                    @userType, @id,
                    "Dotdigital_Account__c", "True",
                    "Consent_Source__c", @consentSource,
                    "Consent_type__c", "Single"
                  )
      ELSEIF @consentCRM != "True" AND @marketingconsent == "False" THEN
        SET @updateUser = UpdateSingleSalesforceObject(
                    @userType, @id,
                    "Dotdigital_Account__c", "True",
                    "Consent_Source__c", @tempoConsentSource,
                    "Consent_type__c", "Single"
                  )
      ENDIF
  ELSE  
  /* Standart consent logic */
    IF @consentCRM != "True" AND @marketingconsent == "True" THEN
      SET @updateContact = UpdateSingleSalesforceObject(
                  @userType, @id,
                  "Dotdigital_Account__c", "True",
                  "Consent_Source__c",@consentSource
                )
    ENDIF
  ENDIF

ENDIF
]%%
-->
<script runat="server">
  }
  catch(e){
    Write(Stringify(e));
  }
</script>
   
    %%[ /* Insert Data, Task Creation */ ]%%  
    <div data-type="slot" data-key="amp1" data-label="Add AMPscript here" data-allowed-blocks="codesnippetblock"></div>
 
   <title>%%=v(@TabTitle)=%%</title>
   
    <style>
    @media (min-width: 768px){
    .form .form__wrapper.embedded {  padding-top: 0em; margin-top: 0em;}
    } 
    .form .form__wrapper.embedded {  padding-top: 0em; margin-top: 0em;}
     [data-key='amp1'], [data-key='amp']{
        background-color: #b3bc007d;
      }
     [data-key='header1']{
        background-color:  #f9d9db94;
      }
      [data-key='maincontent']{
        background-color:  #78c3ed26;
      }
      [data-key='col4']{
        background-color:  #98a6ba33;
      }
      [data-key='col5']{
        background-color:  #fe9f17a3;
      }
      
      .editor-slot-display-design p {
        color: black !important;
      }
      .form fieldset legend {
        margin-bottom: 1em;
      }
      .form fieldset {
        margin-bottom: 2em;
      }
</style>
   <script runat="server">

  if(Variable.GetValue("@needsDoubleOptIn") === "True"){
    // IF DOUBLE OPTIN IS NEEDED THEN THE RECORD WILL BE INJECTED TO THE DOUBLE OPT IN JOURNEY FIRST
    var journeyEntryDefinitionKey = Variable.GetValue("@journeyDefKey")
    var clientId = Variable.GetValue("@clientid")
    var clientSecret = Variable.GetValue("@clientSecret")
    var authURI = Variable.GetValue("@authURI")
    var restURI = Variable.GetValue("@restURI")
    var data = {
      'SubscriberKey': Variable.GetValue("@id"),
      'EmailAddress': Variable.GetValue("@email"),
      'opt_in_status': "False",
      'date_added': Variable.GetValue("@dateAdded")
    }

    var setup = {
      authBaseURI: authURI,
      restBaseURI: restURI,
      clientId: clientId,
      clientSecret: clientSecret,
      eventDefinitionKey: journeyEntryDefinitionKey
   }
    
    

    try {
        var token = getToken(setup);
        var success = false;
        if (!!token) success = triggerEvent(token, setup, data);
    } catch (err) {
        Write("Error: " + Stringify(err));
    }

    function getToken(setup) {

        var config = {
            url : setup.authBaseURI + "v2/token",
            contentType : "application/json",
            payload : {
                "client_id": setup.clientId,
                "client_secret": setup.clientSecret,
                "grant_type": "client_credentials"
            }
        }

        var req = HTTP.Post(config.url, config.contentType, Stringify(config.payload));

        if (req.StatusCode == 200) {
            var res = Platform.Function.ParseJSON(req.Response[0]);
            return res.access_token;
        } else {
            return false;
        }

    }

    function triggerEvent(token, setup, data) {

        var config = {
            url : setup.restBaseURI + "interaction/v1/events",
            contentType : "application/json",
            headerName : ["Authorization"],
            headerValue : ["Bearer " + token],
            payload : {
                ContactKey: data.SubscriberKey,
                EventDefinitionKey: setup.eventDefinitionKey,
                Data: data
            }
        }

        var req = HTTP.Post(config.url, config.contentType, Stringify(config.payload), config.headerName, config.headerValue);

        if (req.StatusCode == 201) {
            var res = Platform.Function.ParseJSON(req["Response"][0]);
            if (res.eventInstanceId != null && res.eventInstanceId != "") return true;
        } else {
            return false;
        }
    }
  }

  }catch(e){
    Write(Stringify(e));
  }
</script>
  </head>
  <body class="%%=v(@standaloneImg)=%%">
    %%[ /* HEADER IMAGE */]%%  
    <header>
      <div data-type="slot" data-key="header1" data-label="Add header image here" data-allowed-blocks="htmlblock; textblock; imageblock; codesnippetblock"></div>
    </header> 
    <!-- MAIN -->
    %%[ /* MAIN CONTENT */]%%
    <main class="viewport__inner ">
      <article class="alchemy form" data-animation-marked="alchemy:0">
        <div class="form__wrapper %%=v(@embeddedcss)=%%">
          <div style="transform: translate(0px, 0px); opacity: 1; width: 100%">
            <form action="%%=RequestParameter('PAGEURL')=%%" method="post" id="form" class="form">
              <!-- %%[IF EMPTY(@display) THEN]%% -->
                   <div data-type="slot" data-key="maincontent" data-label="Add main content blocks here"></div>
              <!-- %%[ELSE]%% -->
              
              %%[ /* THANK YOU MESSAGE */]%%
              <div data-type="slot" data-key="col4" data-label="Add Thank you message here"></div>
              <!-- %%[ENDIF]%% -->
              <input type="hidden" id="DL_formname" name="DL_formname" value="%%=v(@GA4formname)=%%">
               <input type="hidden" id="DL_formtype" name="DL_formtype" value="%%=v(@GA4formtype)=%%">
    
            </form>
          </div>
        </div>
      </article>
    </main>
    
 <!--   %%[
set @scriptStart = Concat('<','script','>')
set @scriptEnd = Concat('</','script','>')
]%% -->
%%=v(@scriptStart)=%%
     <div data-type="slot" data-key="col5" data-label="Add script here" data-allowed-blocks="codesnippetblock"></div>
%%=v(@scriptEnd)=%%
  </body>
</html>
